# Bird Inference

Edge inference pipeline for the Bird Pokedex cyberdeck.

Runs on Raspberry Pi / edge hardware using ONNX Runtime. Captures frames from the camera, runs detector + classifier, and returns species predictions.

## Status

Stub only. Requires ONNX exports from `apps/training/`.
