[project]
name = "bird-geo"
version = "0.1.0"
requires-python = ">=3.11,<3.12"
dependencies = [
    "shapely>=2",
    "pyproj>=3",
    "bird-core",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/bird_geo"]
